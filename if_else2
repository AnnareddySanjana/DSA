#include<iostream>
using namespace std;
int main(){
    char ch;
    cin>>ch;
    if(ch>='a' && ch<='z'){          //can use ASCII values for comparision
        cout<<"Lowercase";
    }else{
        cout<<"Uppercase";
    }
    return 0;
}
